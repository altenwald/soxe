{erl_opts, [debug_info]}.
{deps, []}.

{plugins, [pc]}.

{provider_hooks, [
    {pre, [
        {compile, {pc, compile}},
        {clean, {pc, clean}}
    ]}
]}.

{port_specs, [
    {"priv/soxe_drv.so", ["c_src/soxe.c"]}
]}.

{port_env, [
    {"LDFLAGS", "$LDFLAGS -lsox"}
]}.
